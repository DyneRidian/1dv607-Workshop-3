1./Added the functionality for Stand
2./Fixed the hidden dependencies, removed input scanner from view package.
3./Added strategy pattern "Soft 17" to IHitStrategy.
4./Minor tweeks on bugs like pressing hit wont display lose/win.

Changes from Jonathan:
1. Added IWinnerStrategy interface that can be implemented in classes that specify criteria for dealer winning, through return of a boolean.
2. Added 2 variations of the WinnerStrategy, one where dealer wins if score is equal and one where player wins if score if equal.
3.Created public method that returns max score from player class.
4. GetWinnerRule added to RulesFactory, this returns the rule that want the dealer to use.
5. Dealer now initializes the WinnerStrategy we assign to WinnerRule, in it's contructor.